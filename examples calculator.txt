Welcome to the Calculator!
1. Arithmetic operations (+, -, *, /, %)
2. power(base, indicator)
3. sqrt(number)
4. abs(number)
5. round(number)
history - Show history
Select an operation (or type 'exit' to exit):

1

Enter an arithmetic expression (for example, 2 + 3):

140 + 5

Result: 145.0
Do you want to continue? (y/n):

y

1. Arithmetic operations (+, -, *, /, %)
2. power(base, indicator)
3. sqrt(number)
4. abs(number)
5. round(number)
history - Show history
Select an operation (or type 'exit' to exit):

2


Enter the base and exponent (eg 2, 3):
5, 5

Result: 3125.0
Do you want to continue? (y/n):

y

1. Arithmetic operations (+, -, *, /, %)
2. power(base, indicator)
3. sqrt(number)
4. abs(number)
5. round(number)
history - Show history
Select an operation (or type 'exit' to exit):

2

Enter the base and exponent (eg 2, 3):

5, 2

Result: 25.0
Do you want to continue? (y/n):

y

1. Arithmetic operations (+, -, *, /, %)
2. power(base, indicator)
3. sqrt(number)
4. abs(number)
5. round(number)
history - Show history
Select an operation (or type 'exit' to exit):

3

Enter number:

169

Result: 13.0
Do you want to continue? (y/n):

y

1. Arithmetic operations (+, -, *, /, %)
2. power(base, indicator)
3. sqrt(number)
4. abs(number)
5. round(number)
history - Show history
Select an operation (or type 'exit' to exit):

4

Enter number:

-4

Result: 4.0
Do you want to continue? (y/n):

y

1. Arithmetic operations (+, -, *, /, %)
2. power(base, indicator)
3. sqrt(number)
4. abs(number)
5. round(number)
history - Show history
Select an operation (or type 'exit' to exit):

5

Enter number:

199

Result: 199
Do you want to continue? (y/n):

y

1. Arithmetic operations (+, -, *, /, %)
2. power(base, indicator)
3. sqrt(number)
4. abs(number)
5. round(number)
history - Show history
Select an operation (or type 'exit' to exit):

5

Enter number:

199.5

Result: 200
Do you want to continue? (y/n):

y

1. Arithmetic operations (+, -, *, /, %)
2. power(base, indicator)
3. sqrt(number)
4. abs(number)
5. round(number)
history - Show history
Select an operation (or type 'exit' to exit):

199.9

Incorrect selection. Please try again.
1. Arithmetic operations (+, -, *, /, %)
2. power(base, indicator)
3. sqrt(number)
4. abs(number)
5. round(number)
history - Show history
Select an operation (or type 'exit' to exit):

5

Enter number:

199.9

Result: 200
Do you want to continue? (y/n):

y

1. Arithmetic operations (+, -, *, /, %)
2. power(base, indicator)
3. sqrt(number)
4. abs(number)
5. round(number)
history - Show history
Select an operation (or type 'exit' to exit):

history

Calculation history:
1 = 145.0
2 = 3125.0
2 = 25.0
3 = 13.0
4 = 4.0
5 = 199
5 = 200
5 = 200
1. Arithmetic operations (+, -, *, /, %)
2. power(base, indicator)
3. sqrt(number)
4. abs(number)
5. round(number)
history - Show history
Select an operation (or type 'exit' to exit):